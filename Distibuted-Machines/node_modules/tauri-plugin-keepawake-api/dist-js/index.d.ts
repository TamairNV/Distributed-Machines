export interface KeepAwakeConfig {
    /** Keep display on */
    display: boolean;
    /** Keep system from idle sleeping */
    idle: boolean;
    /** Keep system from sleeping (Functionality and conditions for this to work vary by OS) */
    sleep: boolean;
}
export declare function start(config?: KeepAwakeConfig): Promise<string | null>;
export declare function stop(): Promise<string | null>;
