'use strict';

var core = require('@tauri-apps/api/core');

async function start(config) {
    return await core.invoke('plugin:keepawake|start', { config });
}
async function stop() {
    return await core.invoke('plugin:keepawake|stop');
}

exports.start = start;
exports.stop = stop;
